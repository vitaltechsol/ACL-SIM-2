<Window x:Class="ACL_SIM_2.Views.GlobalSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Global Settings" Height="220" Width="302"
        Background="#1e1e1e" Foreground="White">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="ProSim IP:" Width="160" VerticalAlignment="Center" />
				<TextBox Text="{Binding ProsimIp}" Width="113" />
			</StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Auto connect to ProSim:" Width="160" VerticalAlignment="Center" />
                <CheckBox IsChecked="{Binding AutoConnectProsim}" VerticalAlignment="Center" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Auto center axis on startup:" Width="160" VerticalAlignment="Center" />
                <CheckBox IsChecked="{Binding AutoCenterOnStartup}" VerticalAlignment="Center" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" Width="100" Command="{Binding CloseCommand}" />
            <Button Content="Save" Width="100" Margin="0,0,8,0" Command="{Binding SaveCommand}" />
            
        </StackPanel>
    </Grid>
</Window>
